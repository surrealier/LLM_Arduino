# 🎉 새로운 기능 가이드

## 개요

이 문서는 LLM Arduino 홈 어시스턴트/펫 프로젝트에 추가된 새로운 기능들을 설명합니다.

---

## ✨ 주요 신규 기능

### 1. 감정 표현 시스템 😊

**위치**: `server/emotion_system.py`

펫처럼 감정을 가지고 LED 색상과 서보 동작으로 표현합니다.

#### 지원 감정
- `happy` (행복) - 노란색 LED, 끄덕이기
- `sad` (슬픔) - 파란색 LED, 천천히 좌우
- `excited` (흥분) - 핑크색 LED, 빠르게 흔들기
- `sleepy` (졸림) - 보라색 LED, 천천히 내려가기
- `angry` (화남) - 빨간색 LED, 빠르게 좌우
- `neutral` (중립) - 초록색 LED, 중앙

#### 사용 예시
```python
# 대화 내용에서 자동 감정 분석
emotion = emotion_system.analyze_emotion("정말 기뻐요!")  # -> "happy"

# 감정 명령 생성
command = emotion_system.get_emotion_command()
# -> {"action": "EMOTION", "emotion": "happy", "led": {...}, "servo_action": "NOD"}
```

#### Arduino 통합
ESP32가 감정 명령을 받으면 자동으로:
1. LED 색상 변경
2. 감정에 맞는 서보 동작 수행

---

### 2. 프로액티브 상호작용 💬

**위치**: `server/proactive_interaction.py`

사용자가 말을 걸지 않아도 자발적으로 대화를 시작합니다.

#### 기능
- **시간대별 인사**: "좋은 아침이에요!", "저녁은 드셨나요?"
- **침묵 후 멘트**: "심심해요. 뭐 하세요?"
- **활동 제안**: "날씨가 좋은데 산책은 어때요?"
- **기분 체크**: "기분이 어떠세요?"

#### 설정
`config.yaml`:
```yaml
assistant:
  proactive: true
  proactive_interval: 1800  # 30분마다
```

---

### 3. 정보 서비스 📰

**위치**: `server/info_services.py`

실생활에 유용한 정보를 음성으로 제공합니다.

#### 지원 기능

##### 시간/날짜
```
사용자: "지금 몇 시야?"
AI: "현재 시각은 2026년 1월 13일 14시 30분, 월요일입니다."
```

##### 날씨 정보
```
사용자: "오늘 날씨 어때?"
AI: "서울의 현재 날씨는 맑음입니다. 기온은 5.2도, 체감온도는 3.1도이며, 습도는 45%입니다."
```

**설정 필요**: OpenWeatherMap API 키
1. https://openweathermap.org/api 에서 무료 키 발급
2. `.env` 파일에 추가:
```
WEATHER_API_KEY=your_api_key_here
```

##### 뉴스 헤드라인
```
사용자: "뉴스 알려줘"
AI: "최신 뉴스입니다. 1. [헤드라인1] 2. [헤드라인2] 3. [헤드라인3]"
```

##### 타이머
```
사용자: "3분 타이머 설정해줘"
AI: "타이머 1 타이머를 3분 후로 설정했습니다."

사용자: "타이머 확인"
AI: "활성 타이머: 타이머 1: 2분 30초 남음"
```

---

### 4. 일정 관리 📅

**위치**: `server/scheduler.py`

자연어로 일정을 추가하고 알림을 받습니다.

#### 사용 예시

##### 일정 추가
```
사용자: "내일 오후 3시 회의 있어"
AI: "'회의' 일정을 2026년 1월 14일 15:00에 등록했습니다. 10분 전에 알려드릴게요."

사용자: "모레 오전 10시 병원 가야해"
AI: "'병원' 일정을 2026년 1월 15일 10:00에 등록했습니다."
```

##### 일정 조회
```
사용자: "오늘 일정 뭐야?"
AI: "오늘의 일정 2개입니다. 14:00 팀 미팅, 18:00 저녁 약속"
```

##### 자동 리마인더
일정 10분 전에 자동으로 알림:
```
AI: "📅 15:00에 '회의' 일정이 있습니다!"
```

---

### 5. 통합 설정 관리 ⚙️

**위치**: `server/config.yaml`, `server/config_loader.py`

모든 설정을 한 곳에서 관리합니다.

#### config.yaml 구조
```yaml
# 서버 설정
server:
  host: "0.0.0.0"
  port: 5001

# STT 설정
stt:
  model_size: "small"  # tiny, base, small, medium, large
  device: "cuda"       # cuda or cpu
  language: "ko"

# 어시스턴트 설정
assistant:
  name: "아이"              # 이름
  personality: "cheerful"   # 성격 (cheerful, calm, playful, serious)
  proactive: true
  proactive_interval: 1800

# 날씨 API
weather:
  api_key: ""               # OpenWeatherMap API 키
  location: "Seoul"

# 감정 시스템
emotion:
  enabled: true
  decay_to_neutral: true
  decay_interval: 300

# 로깅
logging:
  level: "INFO"
  save_to_file: true
  log_dir: "logs"
```

#### 환경 변수 (.env)
민감한 정보는 `.env` 파일에:
```bash
WEATHER_API_KEY=your_api_key_here
SERVER_PORT=5001
DEVICE=cuda
ASSISTANT_NAME=아이
LOG_LEVEL=INFO
```

---

### 6. 개성 커스터마이즈 🎭

**위치**: `server/agent_mode_improved.py`

어시스턴트의 이름과 성격을 변경할 수 있습니다.

#### 성격 타입

##### cheerful (밝고 활발)
```
AI: "안녕하세요! 오늘도 좋은 하루 보내세요! 😊"
```

##### calm (차분하고 안정적)
```
AI: "안녕하세요. 편안한 하루 되시길 바랍니다."
```

##### playful (장난기 있고 유쾌)
```
AI: "안녕! 오늘 뭐 재밌는 일 없어? 나랑 놀자!"
```

##### serious (진지하고 전문적)
```
AI: "안녕하십니까. 무엇을 도와드릴까요?"
```

#### 설정 방법
`config.yaml`:
```yaml
assistant:
  name: "토리"           # 원하는 이름
  personality: "playful"  # 원하는 성격
```

---

### 7. 향상된 로깅 📊

**위치**: `server/logger_config.py`

디버깅과 모니터링을 위한 강화된 로깅 시스템.

#### 기능
- **컬러 콘솔 출력**: 레벨별 색상 구분
- **파일 저장**: 일별 로그 파일 자동 생성
- **에러 로그**: 에러만 별도 파일에 저장
- **성능 메트릭**: STT/LLM/TTS 응답 시간 추적

#### 로그 파일
```
logs/
├── app_20260113.log      # 모든 로그
└── error_20260113.log    # 에러만
```

#### 성능 통계
서버 종료 시 (Ctrl+C) 자동 출력:
```
==================================================
Performance Statistics
--------------------------------------------------
STT Requests: 45 (avg: 0.85s)
LLM Requests: 45 (avg: 1.23s)
TTS Requests: 30 (avg: 1.45s)
Errors: 2
==================================================
```

---

## 🚀 시작하기

### 1. 설정 파일 준비

```bash
cd server

# config.yaml 수정 (이미 생성됨)
# .env 파일 생성
cp env.example .env
# .env 파일 편집하여 API 키 입력
```

### 2. 의존성 설치

```bash
pip install -r requirements.txt
```

새로 추가된 패키지:
- `requests` - 날씨 API 호출
- `feedparser` - 뉴스 RSS 파싱
- `python-dotenv` - 환경 변수 관리

### 3. 서버 실행

```bash
python stt_improved.py
```

출력 예시:
```
2026-01-13 14:30:00 | INFO | config_loader | Configuration loaded successfully
2026-01-13 14:30:00 | INFO | stt | Loading STT model: small on cuda...
2026-01-13 14:30:05 | INFO | robot_mode | Robot Mode LLM loaded.
2026-01-13 14:30:10 | INFO | agent_mode | Agent Mode LLM loaded.
2026-01-13 14:30:10 | INFO | agent_mode | Context restored: 15 conversations, 5 memories
2026-01-13 14:30:10 | INFO | stt | 🤖 Assistant: 아이 (cheerful)
2026-01-13 14:30:10 | INFO | stt | 🚀 Server started on 5001. Default Mode: robot
```

### 4. Arduino 업로드

```bash
# config.h 파일 확인 및 수정
# WiFi 정보와 서버 IP 설정
```

---

## 📝 사용 시나리오

### 시나리오 1: 아침 루틴

```
[오전 7시, 프로액티브 메시지]
AI: "좋은 아침이에요! 잘 주무셨나요?"

사용자: "응, 오늘 날씨 어때?"
AI: "서울의 현재 날씨는 맑음입니다. 기온은 -2도입니다."

사용자: "오늘 일정 뭐야?"
AI: "오늘의 일정 2개입니다. 10:00 회의, 14:00 치과"
```

### 시나리오 2: 일정 관리

```
사용자: "내일 오후 3시 병원 가야해"
AI: "'병원' 일정을 2026년 1월 14일 15:00에 등록했습니다. 10분 전에 알려드릴게요."

[다음날 14:50]
AI: "📅 15:00에 '병원' 일정이 있습니다!"
```

### 시나리오 3: 감정 표현

```
사용자: "정말 기쁜 일이 있었어!"
[감정 분석: happy]
[LED가 노란색으로 변경]
[서보가 끄덕이는 동작]
AI: "와! 정말 좋은 일이네요! 축하드려요!"
```

### 시나리오 4: 타이머

```
사용자: "라면 끓일 건데 3분 타이머 맞춰줘"
AI: "타이머 1 타이머를 3분 후로 설정했습니다."

[3분 후]
AI: "⏰ 타이머 1 타이머가 완료되었습니다!"
```

---

## 🔧 고급 설정

### 프로액티브 간격 조정

더 자주 말을 걸게 하려면:
```yaml
assistant:
  proactive_interval: 900  # 15분
```

덜 자주 말을 걸게 하려면:
```yaml
assistant:
  proactive_interval: 3600  # 1시간
```

### 감정 시스템 비활성화

```yaml
emotion:
  enabled: false
```

### 로그 레벨 변경

디버깅 시:
```yaml
logging:
  level: "DEBUG"
```

운영 시:
```yaml
logging:
  level: "WARNING"
```

---

## 🐛 문제 해결

### 날씨 정보가 안 나와요
1. `.env` 파일에 `WEATHER_API_KEY` 확인
2. OpenWeatherMap API 키가 활성화되었는지 확인 (발급 후 몇 시간 소요)
3. `requests` 패키지 설치 확인

### 뉴스가 안 나와요
1. `feedparser` 패키지 설치 확인
2. 인터넷 연결 확인

### 일정이 저장 안 돼요
1. `schedules.json` 파일 권한 확인
2. 로그에서 에러 메시지 확인

### 프로액티브 메시지가 안 나와요
1. `config.yaml`에서 `proactive: true` 확인
2. `proactive_interval` 시간 확인
3. Agent 모드인지 확인 (Robot 모드에서는 작동 안 함)

---

## 📚 API 참조

### EmotionSystem

```python
from emotion_system import EmotionSystem

emotion = EmotionSystem()

# 감정 분석
detected = emotion.analyze_emotion("행복해요!")  # -> "happy"

# LED 색상 가져오기
rgb = emotion.get_led_color("happy")  # -> (255, 200, 0)

# 서보 동작 가져오기
action = emotion.get_servo_action("happy")  # -> "NOD"

# 명령 생성
cmd = emotion.get_emotion_command()
```

### InfoServices

```python
from info_services import InfoServices

info = InfoServices(weather_api_key="your_key", location="Seoul")

# 시간
time_str = info.get_current_time()

# 날씨
weather_str = info.get_weather()

# 뉴스
news_str = info.get_news_headlines(count=3)

# 타이머
msg = info.set_timer(180, label="라면")  # 3분

# 자연어 처리
response = info.process_info_request("지금 몇 시야?")
```

### Scheduler

```python
from scheduler import Scheduler
from datetime import datetime, timedelta

scheduler = Scheduler()

# 일정 추가
tomorrow = datetime.now() + timedelta(days=1)
tomorrow = tomorrow.replace(hour=15, minute=0)
msg = scheduler.add_schedule("회의", tomorrow, reminder_before=10)

# 자연어로 추가
msg = scheduler.parse_and_add_schedule("내일 오후 3시 회의 있어")

# 일정 조회
today_str = scheduler.get_today_schedules()
upcoming = scheduler.get_upcoming_schedules(hours=24)

# 리마인더 확인
reminders = scheduler.check_reminders()
```

---

## 🎯 다음 단계

구현된 모든 기능이 완료되었습니다! 추가로 고려할 수 있는 확장:

1. **웨이크워드 감지**: "헤이 아이" 등으로 활성화
2. **다중 서보 지원**: 더 풍부한 움직임
3. **스마트홈 연동**: MQTT, Home Assistant
4. **웹 대시보드**: 실시간 모니터링
5. **음성 명령 학습**: 사용자 맞춤 명령어

---

## 📄 라이선스

MIT License

## 🤝 기여

이슈 및 PR 환영합니다!
